import { Inter, Lilita_One, Lora } from "next/font/google";

// define your variable fonts
const inter = Inter();
const lora = Lora();
const lilita = Lilita_One({ weight: "400" });

export { inter, lora, lilita };
