// define the types which a variable can take

let str: string = "inder"; // Defined that str will only take string
let num: number = 54; // this will only take a number

interface Person {
  name: string;
  age: number;
}
